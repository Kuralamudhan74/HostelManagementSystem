# Environment
# IMPORTANT: Set to 'production' when deploying to production server
NODE_ENV=production

# Server Configuration
PORT=5000

# Database
# PRODUCTION: Replace <db_password> with your actual MongoDB Atlas password
# Whitelist your server IP in MongoDB Atlas Network Access settings
MONGODB_URI=mongodb+srv://vasu:vasu123@hostelmanagement.eojnszm.mongodb.net/?appName=hostelmanagement

# JWT Configuration
# PRODUCTION: Generate new secrets using: openssl rand -base64 64
# These MUST be changed before production deployment for security
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# File Upload Configuration
UPLOADS_DIR=./uploads
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,application/pdf

# API Key Authentication
# PRODUCTION: Use a strong, randomly generated key (use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
API_KEY=a0af33a9cd272e9b5c93c68beb4e7d369ad5ccf507dc69653e330ef344d58ad3

# CORS Configuration
# PRODUCTION: Change to your production frontend URL (e.g., https://yourdomain.com)
#CORS_ORIGIN=https://staymanager.com
 #CORS_ORIGIN=http://localhost:5173
CORS_ORIGIN=https://super-moonbeam-4dae1f.netlify.app

# Rate Limiting (now handled in code based on NODE_ENV)
# Development: 1000 requests per 15 minutes
# Production: 500 requests per 15 minutes
# Auth routes: 100 (dev) / 20 (prod) per 15 minutes

# ============================================
# PRODUCTION DEPLOYMENT CHECKLIST:
# ============================================
# Before deploying to production, you MUST:
# 1. Change NODE_ENV to 'production'
# 2. Replace MONGODB_URI with new production connection string
# 3. Generate and set new JWT_SECRET (64+ characters)
# 4. Generate and set new JWT_REFRESH_SECRET (64+ characters, different from JWT_SECRET)
# 5. Update CORS_ORIGIN to your production frontend URL
# 6. Ensure this .env file is NOT committed to git
# ============================================
